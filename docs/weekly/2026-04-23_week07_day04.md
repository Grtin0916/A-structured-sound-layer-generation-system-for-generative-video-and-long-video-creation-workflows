# 2026-04-23 Week07 Day04 Mainbase

## 1. Goal

把主基地从“Week07 scorecard 已初版落盘”推进到“可直接引用的收口状态”，重点不是补新实验，而是核对 README、scorecard 文档、sample CSV 与 evidence path 是否一致。

## 2. What was checked

本次核查覆盖以下内容：

- README 顶部三段是否已同步到 Week07
- `docs/evals/scorecard_v1.md` 的字段定义是否清晰
- `artifacts/benchmarks/scorecard_example.csv` 是否覆盖三类 eval_group
- CSV 中的 evidence_path 是否都能在仓库内找到
- 本地工作树是否存在与收口无关的噪音文件

## 3. Verified findings

本次核查确认：

- README 已明确写入 Week07 的 scorecard 初版与 10 条 sample CSV
- `scorecard_v1.md` 已固定 common fields，包括 `eval_group`、`subject`、`case_id`、`input_type`、`status`、`failure_tag`、`metric_key`、`metric_value`、`evidence_path`、`note`
- `scorecard_example.csv` 当前共 10 条样例
- 三类覆盖为：
  - functional: 3
  - performance: 2
  - failure_regression: 5
- 当前 CSV 中 evidence_path 均存在，未发现悬空证据路径
- 本次核查不新增实验，不扩写更大模型，不提前宣称 `/metrics`、Prometheus、Grafana 已完成

## 4. Meaning

这说明主基地当前已经不再处于“缺少 Week07 文件”的状态，而是进入“Week07 证据收口”的阶段。

当前最值钱的结果不是继续重复 generator audit，而是确认：
- scorecard 入口已成立
- sample CSV 已能服务后续引用
- failure regression 已有最小 taxonomy 样例
- README 叙事没有超前声称 Week08 内容

## 5. Not done yet

以下内容仍未完成，不应提前写成已完成：

- `/metrics` 暴露与 Prometheus 抓取
- Grafana dashboard 落盘
- contract / runtime / artifact 指标命名统一
- 更严格统一的 benchmark methodology
- `musicgen-medium` / `melody` / 更大模型的正式比较结论

## 6. Next hard move

主基地下一步应进入：

1. 继续补足 scorecard example 的 evidence coverage
2. 让 README / weekly / scorecard 三处描述保持完全同口径
3. 为 Week08 的 metrics 化与指标命名统一预热，但不提前宣称已完成
